# MeltMesh 数学建模方案

## 1. 建模目标

MeltMesh 的目标不是复刻传统网格布尔，而是建立一个可实时求值的连续空间场，使现有解析几何体与导入 GLB 模型产生具有方向、历史和材质转移的接触溶解。

定义：

- \(A\)：现有球体、圆角盒等解析几何体，作为被吸收相。
- \(B\)：导入并体素化的网格，作为吸收相。
- \(d_A(\mathbf p)\)、\(d_B(\mathbf p)\)：二者的有符号距离函数。
- \(\phi(\mathbf p,t)\)：带历史的局部溶解相场。
- \(M_A\)、\(M_B\)：两类几何的 PBR 材质状态。

期望行为为有方向的：

\[
A\rightarrow B
\]

即 \(A\) 进入 \(B\) 的接触区域后被逐渐侵蚀并由 \(B\) 的材质接管，而 \(B\) 的原始网格和 PBR 材质保持可识别。

## 2. 几何场

### 2.1 解析几何

球体距离场：

\[
d_{sphere}(\mathbf p)=\|\mathbf p-\mathbf c\|-r
\]

圆角盒距离场：

\[
\mathbf q=|\mathbf p-\mathbf c|-\mathbf b+r
\]

\[
d_{box}=\min(\max(q_x,q_y,q_z),0)+\|\max(\mathbf q,0)\|-r
\]

解析几何内部先使用多项式 smooth-min 组合：

\[
h=\operatorname{clamp}\left(\frac12+\frac{b-a}{2k},0,1\right)
\]

\[
\operatorname{smin}(a,b,k)=\operatorname{mix}(b,a,h)-kh(1-h)
\]

### 2.2 导入网格

GLB 网格在 Blender 转换阶段被采样成三维距离体 \(d_B\)。当前默认分辨率为 \(64^3\)，运行时通过 `Data3DTexture` 在 GPU 中采样。

支持浮点线性过滤时使用三线性硬件采样；CPU 接触求解始终采用显式三线性插值，降低最近邻体素跳变。

## 3. 定向布尔吸收

普通对称平滑并集只能把两个物体连接起来：

\[
d=\operatorname{smin}(d_A,d_B,k)
\]

它无法表达谁溶解谁。本项目先围绕导入模型建立吸收前沿：

\[
d_C(\mathbf p,t)=d_B(\mathbf p)-r(\phi)+n(\mathbf p,t)
\]

其中：

- \(r(\phi)\)：随相场强度扩张的侵蚀半径。
- \(n(\mathbf p,t)\)：非周期多尺度噪声。

现有几何被吸收前沿侵蚀：

\[
d_A'(\mathbf p)=\max(d_A(\mathbf p),-d_C(\mathbf p,t))
\]

最终几何场为：

\[
d_{final}(\mathbf p)=
\operatorname{smin}_{k(\mathbf p)}(d_A'(\mathbf p),d_B(\mathbf p))
\]

局部平滑尺度定义为：

\[
k(\mathbf p)=
\operatorname{clamp}
\left(
2.4|d_B(\mathbf p)|+0.1k_0,
0.06k_0,
k_0k_s
\right)
\]

它会在薄壁和接近导入模型表面的区域自动收缩，减少细窗框被 smooth-min 膨胀的问题。

## 4. 局部相场

### 4.1 接触点求解

球体使用中心与六个轴向支撑点；圆角盒使用中心与八个角点。在导入模型 CPU 距离场中选取距离最小的支撑点 \(\mathbf p_s\)。

根据距离场梯度将支撑点投影到真实网格表面：

\[
\mathbf c=
\mathbf p_s-d_B(\mathbf p_s)
\frac{\nabla d_B(\mathbf p_s)}{\|\nabla d_B(\mathbf p_s)\|}
\]

### 4.2 各向异性相场种子

系统维护八个循环接触种子。每个种子保存：

\[
S_i=(\mathbf c_i,\mathbf n_i,a_i)
\]

其中 \(\mathbf n_i\) 是网格表面法线，\(a_i\) 是种子强度。

将空间位移分解为切向和法向：

\[
d_N=(\mathbf p-\mathbf c_i)\cdot\mathbf n_i
\]

\[
d_T^2=\|\mathbf p-\mathbf c_i\|^2-d_N^2
\]

单个种子的局部相场为：

\[
\phi_i(\mathbf p)=a_i
\exp\left(
-\frac{d_T^2}{2\sigma_T^2}
-\frac{d_N^2}{2\sigma_N^2}
\right)
\]

当前取：

\[
\sigma_N=0.34\sigma_T
\]

因此溶解沿网格表面传播较宽，沿法线方向较薄，避免球形相场同时穿透薄壁两侧。

多个种子通过概率并集合成：

\[
\phi(\mathbf p)=1-\prod_i(1-\phi_i(\mathbf p))
\]

相邻轨迹会连续增强，不产生 `max` 运算造成的不可导分界。

### 4.3 时间演化

接触时：

\[
\frac{da_i}{dt}=\alpha(1-a_i)
\]

分离时：

\[
\frac{da_i}{dt}=-\beta a_i
\]

其中 \(\alpha\) 对应“溶解速率”，\(\beta\) 对应“恢复速率”。恢复速率为零时，接触历史保持。

## 5. 多尺度侵蚀噪声

侵蚀前沿使用三频连续值噪声：

\[
n(\mathbf p,t)=
0.56N(3.8\mathbf p+\mathbf v_0t)
+0.30N(9.1\mathbf p+\mathbf v_1t)
+0.14N(21\mathbf p+\mathbf v_2t)
\]

三层分别控制整体溶解轮廓、中尺度侵蚀和细小边缘变化。噪声幅度受局部相场约束，不作用于未接触表面。

## 6. 材质所有权

定义导入材质权重 \(w_B\)：

\[
w_B=\max(w_{absorbed},w_{distance})
\]

其中 \(w_{absorbed}\) 由吸收前沿决定，\(w_{distance}\) 由两距离场的相对优势决定。

最终材质状态：

\[
M=(1-w_B)M_A+w_BM_B
\]

实际参与混合的通道包括颜色与粗糙度。原始 GLB 表面优先由 Three.js PBR 管线渲染；只有融合颈部、侵蚀开口和距离场生成的新表面由烘焙材质场接管。

## 7. 数值求解

相机射线：

\[
\mathbf p(t)=\mathbf o+t\mathbf d
\]

采用保守 Sphere Tracing：

\[
t_{j+1}=t_j+0.82d_{final}(\mathbf p_j)
\]

当前最大步数为 96。由于侵蚀噪声会破坏严格距离性质，步长系数小于 1，以降低射线越过表面的风险。

法线通过中心有限差分计算：

\[
\mathbf n=\frac{\nabla d_{final}}{\|\nabla d_{final}\|}
\]

## 8. 布尔结算器参数

| 参数 | 数学作用 | 视觉影响 |
|---|---|---|
| 融合强度 | 基准尺度 \(k_0\) | 控制整体反应尺度 |
| 接触阈值 | 启动相场积分的距离条件 | 过高会隔空反应 |
| 侵蚀半径 | \(r(\phi)\) 的上限 | 控制旧几何被吸收的深度 |
| 布尔平滑 | \(k_s\) | 控制最终连接颈部宽度 |
| 前沿扰动 | 噪声幅度 | 控制侵蚀边界不规则程度 |
| 溶解速率 | \(\alpha\) | 控制接触积累速度 |
| 恢复速率 | \(\beta\) | 控制分离后的退相速度 |

推荐基准：

```text
融合强度    0.28
接触阈值    0.22
侵蚀半径    1.05
布尔平滑    0.14
前沿扰动    0.18
溶解速率    0.85
恢复速率    0.01
```

## 9. 验收指标

模型优化应使用可观测指标，而不是笼统要求“像 Womp”：

1. 未接触时不得产生几何或材质反应。
2. 初次接触后 100 ms 内出现连续反馈。
3. 薄壁另一侧不得被同一局部相场明显侵蚀。
4. 拖动接触点时不得出现超过一个体素宽度的位置跳变。
5. 导入模型主体保持原始 PBR 材质可识别。
6. 新生成连接面不得出现明显深度穿插。
7. 相邻种子交界处不得出现硬分界线。
8. 1080p 目标帧率不低于 30 FPS。

## 10. 与 Womp 对标的边界

Womp 的生产内核、场表示、体素结构和材质求解器没有公开。本项目仅把 Womp 作为交互与视觉表现参考，没有使用 Womp 源代码、资产或专有实现资料。

当前主要差距：

- 导入距离场仍为固定 \(64^3\)，不是自适应稀疏窄带。
- 局部相场由八个解析种子近似，不是完整三维 PDE 网格。
- 材质体只烘焙颜色和粗糙度，没有完整金属度、法线和透射场。
- 尚未实现自适应步进、局部网格提取和编辑撤销栈。

后续优先级：

1. 局部 \(128^3\) 或自适应窄带 SDF。
2. 低分辨率三维相场纹理与 GPU ping-pong 演化。
3. 完整 PBR 材质体积和材质边界重投影。
4. 基于屏幕误差的自适应 Sphere Tracing。
5. 对接触、薄壁和拖动轨迹建立自动化视觉回归测试。
